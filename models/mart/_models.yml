version: 2

models:
  - name: time_spine
    description: "Continuous daily date spine used by the semantic layer for time-based metrics."
    time_spine:
      standard_granularity_column: date_day
    columns:  
      - name: date_day
        description: "Unique date at day granularity."
        granularity: day